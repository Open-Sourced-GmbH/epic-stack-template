// Owned preview — mirrors the `textarea` specimen.
import { Textarea } from 'epic-stack-template'

export const Default = () => (
	<div className="max-w-sm">
		<Textarea placeholder="Write something…" />
	</div>
)
