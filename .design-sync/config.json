{
	"pkg": "epic-stack-template",
	"projectId": "e6ba00a3-b80a-441b-96b7-317177bf55b1",
	"globalName": "EpicUI",
	"entry": ".design-sync/entry.tsx",
	"srcDir": "app/components/ui",
	"tsconfig": "tsconfig.json",
	"cssEntry": ".design-sync/styles.compiled.css",
	"guidelinesGlob": [],
	"componentSrcMap": {
		"Alert": "app/components/ui/alert.tsx",
		"Avatar": "app/components/ui/avatar.tsx",
		"Badge": "app/components/ui/badge.tsx",
		"Button": "app/components/ui/button.tsx",
		"Card": "app/components/ui/card.tsx",
		"Checkbox": "app/components/ui/checkbox.tsx",
		"Dialog": "app/components/ui/dialog.tsx",
		"Input": "app/components/ui/input.tsx",
		"Label": "app/components/ui/label.tsx",
		"StatusButton": "app/components/ui/status-button.tsx",
		"Skeleton": "app/components/ui/skeleton.tsx",
		"Spinner": "app/components/ui/spinner.tsx",
		"Textarea": "app/components/ui/textarea.tsx",
		"DropdownMenu": "app/components/ui/dropdown-menu.tsx",
		"InputOTP": "app/components/ui/input-otp.tsx",
		"Tooltip": "app/components/ui/tooltip.tsx",
		"Accordion": "app/components/ui/accordion.tsx",
		"Slider": "app/components/ui/slider.tsx",
		"CommandPalette": "app/components/ui/command.tsx",
		"Field": "app/components/ui/field.tsx",
		"Select": "app/components/ui/select.tsx",
		"Pagination": "app/components/ui/pagination.tsx",
		"TagInput": "app/components/ui/tag-input.tsx",
		"Switch": "app/components/ui/switch.tsx",
		"Separator": "app/components/ui/separator.tsx",
		"PageHeader": "app/components/ui/page-header.tsx",
		"FormCard": "app/components/ui/form-card.tsx",
		"Table": "app/components/ui/table.tsx",
		"Sheet": "app/components/ui/sheet.tsx",
		"Sidebar": "app/components/ui/sidebar.tsx",
		"ToggleChip": "app/components/ui/toggle-chip.tsx"
	},
	"dtsPropsFor": {
		"Alert": "/** Alert / Callout for form- and page-level messages. Compose: Alert (root) wrapping an AlertTitle and an AlertDescription. Tokens only (brand / muted / destructive families) — no new theme values. `error` is announced assertively (role=\"alert\"); the other tones use role=\"status\", and an explicit `role` prop wins. */\n/** Message tone. @default 'info' */\ntone?: 'info' | 'success' | 'warning' | 'error';\nrole?: 'alert' | 'status';\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"Avatar": "/** Avatar — a user image with a graceful initials fallback, on the Radix base. Compose: Avatar (root) wrapping an AvatarImage (the photo) and an AvatarFallback (the initials shown until the image loads, or for good if it never does — a missing/slow image degrades to initials, not a broken-image glyph). Tokens only (the fallback uses the muted pair). Size it with utility classes on the root (`size-8`, `size-16`, …); the base is a `size-10` circle. */\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <span> attributes. */\n[key: string]: unknown;",
		"Badge": "/** Status pill for rows/lists — a small rounded label. The tonal `brand` variant (faint --brand-soft fill + --brand text) backs Published/Draft/\"Enabled\" status pills; pair it with `dot` for a leading status dot that tracks the variant's text color (bg-current), so a Draft pill is the muted/secondary variant + dot. Tokens only; no new theme values. */\n/** Visual style variant. @default 'default' */\nvariant?: 'default' | 'secondary' | 'destructive' | 'outline' | 'brand';\n/** Render a leading status dot that tracks the variant's text color. @default false */\ndot?: boolean;\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <span> attributes. */\n[key: string]: unknown;",
		"Button": "/** Visual style variant. @default 'default' */\nvariant?: 'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link';\n/** Size preset. @default 'default' */\nsize?: 'default' | 'wide' | 'sm' | 'lg' | 'pill' | 'icon' | 'icon-sm' | 'icon-lg';\n/** Render as the child element via Radix Slot instead of a native <button>. @default false */\nasChild?: boolean;\nchildren?: React.ReactNode;\nclassName?: string;\ndisabled?: boolean;\ntype?: 'button' | 'submit' | 'reset';\nonClick?: React.MouseEventHandler<HTMLButtonElement>;\n/** Plus all native <button> attributes. */\n[key: string]: unknown;",
		"Card": "/** Compound card surface promoting the `bg-card text-card-foreground rounded-lg border` idiom. Compose: Card (root) wrapping an optional CardHeader (holding CardTitle + CardDescription), a CardContent, and an optional CardFooter. Tokens only — no hardcoded colors or radii. */\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"Checkbox": "/** Controlled checked state; 'indeterminate' for a mixed state. */\nchecked?: boolean | 'indeterminate';\n/** Uncontrolled initial checked state. */\ndefaultChecked?: boolean;\nonCheckedChange?: (checked: boolean | 'indeterminate') => void;\ndisabled?: boolean;\nrequired?: boolean;\nname?: string;\nvalue?: string;\nid?: string;\nclassName?: string;\n/** Apply the invalid styling (red border). */\n'aria-invalid'?: boolean | 'true' | 'false';",
		"Dialog": "/** Shared modal overlay (Radix). The deliberate ADR 023 exception for transient client overlays (⌘K palette, confirm step) — not content-bearing or bookmarkable; reach for a page when the content should be linkable. Compose: Dialog (root) wrapping a DialogTrigger, an optional DialogOverlay, and a DialogContent holding a DialogTitle (+ optional DialogDescription) and a DialogClose. Tokens only (popover / border). */\n/** Controlled open state. */\nopen?: boolean;\n/** Uncontrolled initial open state. */\ndefaultOpen?: boolean;\nonOpenChange?: (open: boolean) => void;\n/** Trap focus and block outside interaction while open. @default true */\nmodal?: boolean;\nchildren?: React.ReactNode;",
		"Input": "type?: React.HTMLInputTypeAttribute;\nplaceholder?: string;\nvalue?: string;\ndefaultValue?: string;\ndisabled?: boolean;\nrequired?: boolean;\nreadOnly?: boolean;\nname?: string;\nid?: string;\nclassName?: string;\nonChange?: React.ChangeEventHandler<HTMLInputElement>;\n/** Apply the invalid styling (red border). */\n'aria-invalid'?: boolean | 'true' | 'false';\n/** Plus all native <input> attributes. */\n[key: string]: unknown;",
		"Label": "/** Associates the label with a form control by its id. */\nhtmlFor?: string;\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <label> attributes. */\n[key: string]: unknown;",
		"StatusButton": "/** Drives the trailing status indicator: spinner / check / cross. */\nstatus: 'idle' | 'pending' | 'success' | 'error';\n/** Optional tooltip message shown on the status indicator. */\nmessage?: string | null;\n/** Tuning for the pending-spinner delay (see the `spin-delay` package). */\nspinDelay?: { delay?: number; minDuration?: number };\n/** Inherited from Button. @default 'default' */\nvariant?: 'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link';\n/** Inherited from Button. @default 'default' */\nsize?: 'default' | 'wide' | 'sm' | 'lg' | 'pill' | 'icon' | 'icon-sm' | 'icon-lg';\nchildren?: React.ReactNode;\nclassName?: string;\ndisabled?: boolean;\ntype?: 'button' | 'submit' | 'reset';\nonClick?: React.MouseEventHandler<HTMLButtonElement>;\n/** Plus all native <button> attributes. */\n[key: string]: unknown;",
		"Skeleton": "/** Muted, pulsing placeholder for loading/empty states. Size and shape it with utility classes (`h-4 w-32`, `rounded-full`, …); the base supplies the `bg-muted` fill, a default `rounded`, and the pulse animation (dropped under prefers-reduced-motion). Tokens only. */\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"Spinner": "/** Standalone inline loading indicator: a spinning `update` icon in a `role=\"status\"` region (rotation dropped under prefers-reduced-motion). Backs any loading/empty state; StatusButton composes it for its pending state. */\n/** Accessible label for the status region / icon. @default 'loading' */\ntitle?: string;\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"Textarea": "placeholder?: string;\nvalue?: string;\ndefaultValue?: string;\nrows?: number;\ndisabled?: boolean;\nrequired?: boolean;\nreadOnly?: boolean;\nname?: string;\nid?: string;\nclassName?: string;\nonChange?: React.ChangeEventHandler<HTMLTextAreaElement>;\n/** Apply the invalid styling (red border). */\n'aria-invalid'?: boolean | 'true' | 'false';\n/** Plus all native <textarea> attributes. */\n[key: string]: unknown;",
		"DropdownMenu": "/** Compound (Radix) menu. Compose: DropdownMenuTrigger + DropdownMenuContent containing DropdownMenuLabel / DropdownMenuSeparator / DropdownMenuItem / DropdownMenuCheckboxItem / DropdownMenuRadioGroup+DropdownMenuRadioItem / DropdownMenuSub(+SubTrigger/+SubContent) / DropdownMenuShortcut. */\n/** Controlled open state. */\nopen?: boolean;\n/** Uncontrolled initial open state. */\ndefaultOpen?: boolean;\nonOpenChange?: (open: boolean) => void;\n/** Block outside interaction while open. @default true */\nmodal?: boolean;\ndir?: 'ltr' | 'rtl';\nchildren?: React.ReactNode;",
		"InputOTP": "/** One-time-code input (the `input-otp` package). Compose the boxes with InputOTPGroup + InputOTPSlot (index 0..maxLength-1), optionally split by InputOTPSeparator. */\n/** Number of character slots. */\nmaxLength: number;\nvalue?: string;\ndefaultValue?: string;\nonChange?: (value: string) => void;\nonComplete?: (value: string) => void;\ndisabled?: boolean;\n/** Regex source constraining allowed characters. */\npattern?: string;\nclassName?: string;\ncontainerClassName?: string;\nchildren?: React.ReactNode;\n/** Apply the invalid styling (red slot borders). */\n'aria-invalid'?: boolean | 'true' | 'false';",
		"Tooltip": "/** Compound (Radix) tooltip. Compose: TooltipTrigger (use asChild to wrap your own control) + TooltipContent. A TooltipProvider is applied automatically by the wrapper. */\n/** Controlled open state. */\nopen?: boolean;\ndefaultOpen?: boolean;\nonOpenChange?: (open: boolean) => void;\n/** Hover delay (ms) before opening. */\ndelayDuration?: number;\nchildren?: React.ReactNode;",
		"Accordion": "/** Compound (Radix) disclosure. Compose: Accordion (root) wrapping one or more AccordionItem (each with a unique `value`), and inside each item an AccordionTrigger + AccordionContent. Styled with design tokens only; the open trigger brand-tints and its plus icon rotates 45°. */\n/** Open behavior. @default 'single' (collapsible) */\ntype?: 'single' | 'multiple';\n/** Controlled open value(s) — a string for single, string[] for multiple. */\nvalue?: string | string[];\n/** Uncontrolled initial open value(s). */\ndefaultValue?: string | string[];\nonValueChange?: (value: string | string[]) => void;\n/** Single-only: allow closing the open item by clicking it again. @default true */\ncollapsible?: boolean;\ndir?: 'ltr' | 'rtl';\norientation?: 'horizontal' | 'vertical';\nchildren?: React.ReactNode;\nclassName?: string;",
		"Slider": "/** Slider on the Radix base (keyboard + ARIA for free). Presents a friendly API over Radix's array form: pass a number for the default single thumb, or [lo, hi] for a two-thumb range — onChange reports the same shape it was given. */\n/** Controlled value — a number, or [lo, hi] for range (two-thumb) mode. */\nvalue?: number | number[];\n/** Uncontrolled initial value (number or [lo, hi]). */\ndefaultValue?: number | number[];\n/** Fires with the new value as a thumb moves — a number, or [lo, hi]. */\nonChange?: (value: number | number[]) => void;\n/** Render the current value(s) in an output alongside the track (pairs with Field for label + live value). */\nshowValue?: boolean;\n/** Customize the value-output text; defaults to `n` or `lo – hi`. */\nformatValue?: (value: number | number[]) => React.ReactNode;\nmin?: number;\nmax?: number;\nstep?: number;\ndisabled?: boolean;\n/** CSS background painted on the track (e.g. an oklch hue / chroma / light sweep); when set, the filled range goes transparent so the gradient reads as the whole track. */\ntrackGradient?: string;\n/** Accessible label forwarded to the thumb(s) (the elements with `slider` role). */\n'aria-label'?: string;\n'aria-labelledby'?: string;\n/** Apply the invalid styling (red thumb border). */\n'aria-invalid'?: boolean | 'true' | 'false';\nclassName?: string;",
		"CommandPalette": "/** Self-contained ⌘K command palette. Pass a `commands` registry and it renders the searchable, grouped, keyboard-navigable list. Omit `open` to render inline (a bordered card — used by the styleguide and tests); pass `open`/`onOpenChange` to render as a focus-trapped dialog overlay. The built-in matcher owns all ranking/grouping/filtering. */\n/** The registry to search. Each command is { id, title, group, keywords?, icon? } plus EITHER `href` (navigation) XOR `run` (action) — never both. */\ncommands: Array<{ id: string; title: string; group: string; keywords?: string[]; icon?: string; href?: string; run?: () => void }>;\n/** When provided, renders as a controlled dialog overlay instead of inline. */\nopen?: boolean;\nonOpenChange?: (open: boolean) => void;\n/** Navigate for `href` commands. @default full-page assignment */\nonNavigate?: (href: string) => void;\n/** Suggested-action chips for the empty / no-match state (each fires its `run`). */\nemptyActions?: Array<{ id: string; title: string; group: string; icon?: string; run: () => void }>;\n/** @default 'Type a command or search…' */\nplaceholder?: string;",
		"Field": "/** Field composite: Label + control + optional description + error, with the aria wiring done for you. Pass the control (Input / Textarea / Select / Slider / …) as the single child; Field sets its `id` to `htmlFor`, and when `error` is set marks it `aria-invalid` and links it via `aria-describedby` to the error (and the description, when present). */\n/** Visible label content. */\nlabel: React.ReactNode;\n/** Id wired to both the Label's `htmlFor` and the control's `id`. */\nhtmlFor: string;\n/** Optional helper text, rendered under the control and linked via `aria-describedby`. */\ndescription?: React.ReactNode;\n/** When set, the message is shown and the control becomes `aria-invalid` + linked to it via `aria-describedby`. */\nerror?: React.ReactNode;\n/** Conveys a required field (asterisk + `aria-required` on the control). */\nrequired?: boolean;\n/** The control element (Input, Textarea, Select, Slider, …). */\nchildren: React.ReactElement;\nclassName?: string;",
		"Select": "/** Labelled dropdown on the Radix base (keyboard + ARIA for free): the trigger is a role=\"combobox\", the panel a role=\"listbox\". Compose: Select (root) wrapping a SelectTrigger (holding a SelectValue) and a SelectContent of SelectItems — optionally bucketed into SelectGroups with a SelectLabel and split by a SelectSeparator. The trigger matches Input exactly (border/background tokens, height, focus ring) and honours the same invalid-state contract: aria-invalid repaints the border with --input-invalid. */\n/** Controlled selected value. */\nvalue?: string;\n/** Uncontrolled initial value. */\ndefaultValue?: string;\nonValueChange?: (value: string) => void;\n/** Controlled open state of the panel. */\nopen?: boolean;\ndefaultOpen?: boolean;\nonOpenChange?: (open: boolean) => void;\ndisabled?: boolean;\nrequired?: boolean;\nname?: string;\ndir?: 'ltr' | 'rtl';\nchildren?: React.ReactNode;",
		"Pagination": "/** Numeric, link-based pager for server-paginated feeds (the /blog index and tag archives). Real <a> links keep deep pages crawlable; the consumer owns the URL shape via getPageHref. The visible window is first + last + current ±1, with collapsed runs shown as an ellipsis; the active page is aria-current=\"page\" on --brand, and prev/next disable at the ends. Wrapped in <nav aria-label=\"Pagination\">; a single page renders nothing. Sizes from --pager-size; colors from --border/--accent/--brand only. */\n/** The current 1-based page. */\npage: number;\n/** Total number of pages. */\npageCount: number;\n/** Map a page number to its href (e.g. `/blog?page=2`). */\ngetPageHref: (page: number) => string;\nclassName?: string;\n/** Plus all native <nav> attributes. */\n[key: string]: unknown;",
		"TagInput": "/** Resolve-or-create multi-select combobox — the editor's tag field (no other multi-select exists; Command/Select are single-select). Selected tags render as removable chips on --secondary; typing opens a --popover menu of matching suggestions plus a \"Create «query»\" row on --brand. Identity is by slug, so \"React\"/\"react\" are one tag and can't be added twice (the server's resolveTags applies the same rule). Full keyboard: ↓/↑ move the active option, Enter selects/creates, Backspace on an empty query removes the last chip. Works controlled (value + onChange) or uncontrolled (defaultValue); with a `name` it emits one hidden input per tag so a plain <Form> submits the selection as repeated keys. Sizes from --combobox-min-h. */\n/** Form field name — one hidden <input> is emitted per selected tag. */\nname?: string;\n/** Existing tag names offered in the menu (filtered by the query). */\nsuggestions?: string[];\n/** Controlled selected tag names. */\nvalue?: string[];\n/** Uncontrolled initial selection. */\ndefaultValue?: string[];\nonChange?: (tags: string[]) => void;\nid?: string;\nplaceholder?: string;\ndisabled?: boolean;\nclassName?: string;\n'aria-label'?: string;\n'aria-labelledby'?: string;",
		"Switch": "/** Switch on the Radix base (role=\"switch\", aria-checked, and Space/Enter keyboard for free). A token-styled toggle: bg-muted off, bg-brand on, a white thumb that slides on a ~200ms transition (dropped under prefers-reduced-motion), 50% opacity when disabled, cosy-focus halo on focus-visible. Powers the dark-mode toggle (compose with a label + sun/moon Icon) and the product-email preference. */\n/** Controlled checked state. */\nchecked?: boolean;\n/** Uncontrolled initial checked state. */\ndefaultChecked?: boolean;\nonCheckedChange?: (checked: boolean) => void;\ndisabled?: boolean;\nrequired?: boolean;\nname?: string;\nvalue?: string;\nid?: string;\nclassName?: string;\n/** Accessible label for the toggle. */\n'aria-label'?: string;\n'aria-labelledby'?: string;",
		"Separator": "/** Separator on the Radix base — the plain rule carries role=\"separator\" (and aria-orientation when vertical) for free and paints a 1px --border line that divides settings sections. Pass a `label` to get the composed labeled variant (\"or continue with\"): a flex row of flex-1 hairline · centered uppercase muted label · flex-1 hairline, with the flanking rules marked decorative so only the label is announced; it sits between the auth email form and the OAuth/passkey rows. Tokens only (--border, --muted-foreground). */\n/** Orientation of the rule. @default 'horizontal' */\norientation?: 'horizontal' | 'vertical';\n/** When set, renders the labeled variant (hairline · label · hairline). */\nlabel?: React.ReactNode;\n/** Marks the plain rule purely decorative (role=\"none\"). */\ndecorative?: boolean;\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"PageHeader": "/** PageHeader — the one page-header pattern reused across all three shells (auth / settings / admin). A flex items-end justify-between row: left = an optional eyebrow (--brand, uppercase) over the title (text-h4), right = an optional actions slot (flex gap-2, e.g. an outline + primary button). The title renders as an <h2> by default; pass headingLevel to place it at the right level for its shell (e.g. 1 for the auth card). Tokens only — no hardcoded colors or sizes. */\n/** Small uppercase brand label above the title. */\neyebrow?: React.ReactNode;\n/** The page title. */\ntitle: React.ReactNode;\n/** Trailing actions (e.g. outline + primary buttons). */\nactions?: React.ReactNode;\n/** Heading level for the title. @default 2 */\nheadingLevel?: 1 | 2 | 3 | 4 | 5 | 6;\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"FormCard": "/** FormCard — the one card surface that frames both an auth form (header optional, centered) and a settings section (header with title + description). bg-card text-card-foreground border border-border at rounded-xl, with an optional header (title text-h6 + optional text-muted-foreground description, divided from the body by a border) and a padded body. The destructive variant applies the destructive-zone pattern (faint --destructive surface, stronger --destructive hairline, text-destructive title) for \"delete account & data\" style sections. The title renders as an <h2> by default; pass headingLevel to place it at the right level for its shell. Tokens only — no hardcoded colors or sizes. */\n/** Optional header title (text-h6). */\ntitle?: React.ReactNode;\n/** Optional header description under the title. */\ndescription?: React.ReactNode;\n/** Surface variant. @default 'default' */\nvariant?: 'default' | 'destructive';\n/** Heading level for the title. @default 2 */\nheadingLevel?: 1 | 2 | 3 | 4 | 5 | 6;\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <div> attributes. */\n[key: string]: unknown;",
		"Table": "/** The branded data table both admin lists (blog + cache) converge onto — bespoke (no Radix), grid-based rows carrying role=\"table\"/\"row\"/\"columnheader\"/\"cell\" for a11y. Composes Checkbox for selection via useRowSelection (the select-all header goes indeterminate for a strict subset; a selected row gets an inset --brand left bar), the tonal Badge for status pills, a ghost icon-sm Button + DropdownMenu for the ⋯ row action, and a footer slot for Pagination. Renders a dashed brand-tiled empty state and Skeleton loading rows. Per-consumer column templates are a prop; zebra striping is opt-in. Tokens only. */\n/** Column definitions, left-to-right. */\ncolumns: Array<{ key: string; header: React.ReactNode; cell: (row: Row) => React.ReactNode; className?: string; headerClassName?: string }>;\n/** Row data. */\ndata: Row[];\n/** Stable id for a row (drives React keys + selection). */\ngetRowId: (row: Row) => string;\n/** grid-template-columns for the data columns; the selection/action tracks are added automatically around it. */\ncolumnTemplate: string;\n/** Row-selection state from useRowSelection — enables the select-all + per-row checkboxes. */\nselection?: { isSelected: (id: string) => boolean; toggle: (id: string) => void; toggleAll: () => void; allSelected: boolean; someSelected: boolean };\n/** Accessible label for a row's selection checkbox (e.g. the row title). */\ngetRowLabel?: (row: Row) => string;\n/** Per-row overflow-menu content (a fragment of DropdownMenuItems). */\nrowActions?: (row: Row) => React.ReactNode;\n/** Accessible label for a row's ⋯ action trigger. @default 'Row actions' */\ngetRowActionsLabel?: (row: Row) => string;\n/** Branded dashed empty state shown when data is empty and not loading. */\nemptyState?: { icon?: React.ReactNode; title: React.ReactNode; description?: React.ReactNode; action?: React.ReactNode };\n/** Render Skeleton placeholder rows instead of the data. */\nloading?: boolean;\n/** Number of Skeleton rows to show while loading. @default 3 */\nloadingRows?: number;\n/** Tint alternating rows. @default false */\nzebra?: boolean;\n/** Footer slot rendered beneath the rows (e.g. a <Pagination/>). */\nfooter?: React.ReactNode;\nclassName?: string;\n'aria-label'?: string;",
		"Sheet": "/** Slide-over panel on the Radix Dialog base (no new dependency) — focus trap, esc-to-close, role=\"dialog\" labelled by its SheetTitle, outside-click / SheetClose dismissal. Anchors to an edge as a full-height rail (`side=\"left\" | \"right\"`) instead of a centered card — the foundation the section Sidebar's mobile drawer mounts in. Compose: Sheet (root) wrapping a SheetTrigger, an optional SheetOverlay, and a SheetContent holding a SheetTitle (+ optional SheetDescription) and a SheetClose. Tokens only (popover / border); rendered in place (no Portal). */\n/** Controlled open state. */\nopen?: boolean;\n/** Uncontrolled initial open state. */\ndefaultOpen?: boolean;\nonOpenChange?: (open: boolean) => void;\n/** Trap focus and block outside interaction while open. @default true */\nmodal?: boolean;\nchildren?: React.ReactNode;",
		"Sidebar": "/** Shared, config-driven section navigation for the account / admin shells (the AppShell sidebar slot). A desktop rail of grouped items (faint --brand-soft wash, border-r) and, below the md breakpoint, a hamburger that opens the SAME grouped items in a Sheet slide-over drawer — rail and drawer never drift. The active item (and its group label) is highlighted by the pure active-section resolver (`activeItemTo` / `isActiveSection`) fed the current pathname; the longest match wins so a nested route lights up its section, not a shorter sibling. Router-agnostic: items render through `linkComponent` (default a plain <a href={to}>); the app passes react-router's Link for client transitions. Tokens only. */\n/** Grouped navigation items: Array<{ label?: string; items: Array<{ to: string; label: string; icon: IconName }> }>. */\ngroups: Array<{ label?: string; items: Array<{ to: string; label: string; icon: string }> }>;\n/** The current location pathname, driving the active-section highlight. */\npathname: string;\n/** Accessible label for the nav + mobile drawer (e.g. \"Account\", \"Admin\"). @default 'Section' */\nlabel?: string;\n/** Link element each item renders as. @default a plain <a href={to}> */\nlinkComponent?: React.ComponentType<{ to: string; className?: string; children: React.ReactNode }>;\nclassName?: string;",
		"ToggleChip": "/** ToggleChip — a controlled two-state toggle button (aria-pressed) styled as a compact chip; backs the per-grant cells of the permission grant matrix (one chip per own/any scope). No existing primitive fits: Switch slides, Checkbox is a box, Button has no pressed state. Three token-only states: off (border-border bg-background text-muted-foreground), on (bg-brand border-brand text-primary-foreground), and locked (bg-brand-soft text-brand + lock glyph) for admin-floor-protected / system-role partial-lock cells. A locked chip is non-toggleable but stays focusable (aria-disabled, not disabled) and, with lockedReason, wraps in a Tooltip explaining why. Controlled only; being a native <button>, Space/Enter toggle it for free and focus-visible takes the cosy-focus halo. Tokens only. */\n/** Controlled pressed (on) state. */\npressed: boolean;\n/** Fires with the next pressed state on toggle (never while locked). */\nonPressedChange?: (pressed: boolean) => void;\n/** Render the non-toggleable locked state (brand-soft tonal + lock glyph). @default false */\nlocked?: boolean;\n/** Explanation shown in a tooltip on the locked chip. */\nlockedReason?: React.ReactNode;\nchildren?: React.ReactNode;\nclassName?: string;\n/** Plus all native <button> attributes. */\n[key: string]: unknown;"
	},
	"readmeHeader": ".design-sync/conventions.md"
}
